
<% @artist.songs.each do |song| %>
  <a href='/songs/<%= song.slug %>' id="song_name"><%= song.name %></a>
<% end %>

<% @artist.genres.each do |genre| %>
  <a href='/genres/<%= genre.slug %>' id="genre_name"><%= genre.name %></a>
<% end %>
